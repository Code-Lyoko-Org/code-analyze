# ============================================
# Code Analyze - AI Code Review Agent
# ============================================
# Copy this file to .env and fill in your values

# LLM Configuration (Required)
# -------------------------------------------
# OpenAI API Key (get from https://platform.openai.com/api-keys)
LLM_API_KEY=sk-your-openai-key-here

# Model to use (recommended: gpt-4o-mini for cost efficiency, gpt-4o for best quality)
LLM_MODEL=gpt-4o-mini

# Embedding Configuration (Optional - defaults to LLM_API_KEY)
# -------------------------------------------
# Uses LLM_API_KEY by default, set separately if using different keys
# EMBEDDING_API_KEY=sk-your-embedding-key-here
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# Langfuse Observability (Optional)
# -------------------------------------------
# Get keys from https://cloud.langfuse.com
# LANGFUSE_PUBLIC_KEY=pk-lf-xxxxx
# LANGFUSE_SECRET_KEY=sk-lf-xxxxx
# LANGFUSE_HOST=https://cloud.langfuse.com

# Docker Settings (Internal - usually no need to change)
# -------------------------------------------
QDRANT_HOST=qdrant
QDRANT_PORT=6333
REDIS_URL=redis://redis:6379/0
TEMP_DIR=/tmp/code-analyze
